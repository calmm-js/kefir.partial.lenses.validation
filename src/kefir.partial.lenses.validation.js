import * as V from 'partial.lenses.validation'
import {liftRec} from 'kefir.combines'

export const accept = liftRec(V.accept)
export const acceptAs = liftRec(V.acceptAs)
export const acceptWith = liftRec(V.acceptWith)
export const accepts = liftRec(V.accepts)
export const acceptsAsync = liftRec(V.acceptsAsync)
export const and = liftRec(V.and)
export const args = liftRec(V.args)
export const arrayId = liftRec(V.arrayId)
export const arrayIx = liftRec(V.arrayIx)
export const both = liftRec(V.both)
export const cases = liftRec(V.cases)
export const casesOf = liftRec(V.casesOf)
export const choose = liftRec(V.choose)
export const dependentFn = liftRec(V.dependentFn)
export const either = liftRec(V.either)
export const errors = liftRec(V.errors)
export const errorsAsync = liftRec(V.errorsAsync)
export const freeFn = liftRec(V.freeFn)
export const ifElse = liftRec(V.ifElse)
export const keep = liftRec(V.keep)
export const lazy = liftRec(V.lazy)
export const modifyAfter = liftRec(V.modifyAfter)
export const modifyError = liftRec(V.modifyError)
export const not = liftRec(V.not)
export const optional = liftRec(V.optional)
export const or = liftRec(V.or)
export const promote = liftRec(V.promote)
export const props = liftRec(V.props)
export const propsOr = liftRec(V.propsOr)
export const reject = liftRec(V.reject)
export const rejectAs = liftRec(V.rejectAs)
export const rejectWith = liftRec(V.rejectWith)
export const remove = liftRec(V.remove)
export const removeAfter = liftRec(V.removeAfter)
export const run = liftRec(V.run)
export const setAfter = liftRec(V.setAfter)
export const setError = liftRec(V.setError)
export const tryValidateAsyncNow = liftRec(V.tryValidateAsyncNow)
export const tuple = liftRec(V.tuple)
export const upgrades = liftRec(V.upgrades)
export const upgradesOf = liftRec(V.upgradesOf)
export const validate = liftRec(V.validate)
export const validateAsync = liftRec(V.validateAsync)
export const where = liftRec(V.where)
